extends ../layout

block content
  h2= book.title
  include by_line
  p= book.body

  div.actions
    a(href="/books") &larr; Back
    span | 
    a(href="/books/" + book.id + "/edit") Edit Book
    span | 
    a(href="/books/" + book.id + "/delete") Delete Book
